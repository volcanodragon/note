线性表中的元素具有一对一的关系，即除开始和结束元素外的其他元素有且只有一个直接前驱[^1]和直接后继[^2]。

线性表的特点有：

- 有且只有一个开始元素；
- 有且只有一个结束元素；
- 除开始元素外所有元素有唯一的直接前驱；
- 除末结束素外所有元素有唯一的直接后继。











[^1]: 元素左侧相邻元素被称为直接前驱，左侧所有元素被称为前驱元素。
[^2]: 元素右侧相邻元素被称为直接后继，右侧所有元素被称为后继元素。

